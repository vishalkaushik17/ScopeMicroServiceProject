namespace GenericFunction.Enums
{
    public enum SPAResponseType
    {
        HTML = 1,
        JSON = 2
    }
    public enum SPAMessageType
    {
        INFO = 1,
        ERR = 2,
        WAR = 3,
    }

    public enum AuthStatus
    {
        Authorized = 700,
        UnAuthorized =400,
    }
}
