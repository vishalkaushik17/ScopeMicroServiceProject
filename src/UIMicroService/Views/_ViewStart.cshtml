@using ModelTemplates.RequestNResponse.Accounts
@inject IHttpContextAccessor HttpContextAccessor
@{
    Layout = (HttpContextAccessor?.HttpContext).IsSignedIn() ? "Layouts/_AdminLteLayout" : "Layouts/_IdentityLayout";
}